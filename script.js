function startCourse(courseId) {
    // You can replace this with actual logic to load course content
    alert(`Starting Course ${courseId}`);
    // You could redirect to a dedicated course page or load content dynamically
  }
  